---
name: "registry-minimal"
version: "1.0.0"
description: "A minimal valid registry skill for testing the validator"
triggers:
  globs:
    - "src/**/*.ts"
  intents:
    - "test minimal registry"
  default_mode: "auto"
dependencies: []
---

# Registry Minimal

A valid registry skill with the bare minimum required fields.
